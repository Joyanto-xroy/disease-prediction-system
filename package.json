{
  "name": "clinical-support-system",
  "private": true,
  "scripts": {
    "dev": "npx http-server public -p 3000 -o",
    "start": "node server.js",
    "build": "echo 'Build completed successfully'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
